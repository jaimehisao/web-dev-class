<h1>Update Blog Post</h1>
{{#if errors.length}}
<div class="alert alert-danger" role="alert">
    <p>Couldn't update the blog post because:</p>
    <ul>
        {{#each errors}}
            <li>{{this}}</li>
        {{/each}}
    </ul>
</div>
{{/if}}

<form action="/blog/{{id}}/update" method="POST">
    <input type="hidden" name="_csrf" value="{{csrfToken}}">
    <div>
        <label for="title">Update Title:</label>
        <textarea name="title" class="form-control">{{blogPost.title}}</textarea>
        <label for="textEntry">Update post:</label>
        <textarea name="textEntry" rows="3" class="form-control" >{{blogPost.textEntry}}</textarea>
    </div>
    <div>
        <input type="submit" value="Update" class="btn btn-primary">
    </div>
</form>